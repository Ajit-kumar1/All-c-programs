#include<stdio.h>
	void main()
	{
		int a,i;
		printf("Enter a number:");
		scanf("%d",&i);
		a=fact(i);
		printf(" = %d",a);
	}
	fact(int a)
	{
		
		if(a<1)
		return 1;
		
		else
		{
			printf("%d * ",a);
		a=a*fact(a-1);
		}
		return a;
	}
